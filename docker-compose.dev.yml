services:
  db:
    image: postgres:16
    container_name: postgres_dev
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_dev:
